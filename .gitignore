# Julia package manifest — not committed for libraries;
# let each Julia version resolve its own compatible dependencies.
Manifest.toml

# Documenter.jl build output — generated by CI; not committed.
docs/build/
